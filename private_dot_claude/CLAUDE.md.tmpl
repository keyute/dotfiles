{{ template "agent-instructions.md" (dict "self" "claude" "root" .) }}
- For sandbox or permission questions (why a path/command/domain is blocked, how to
  allow something), read `~/.claude/docs/sandbox.md` before spawning a lookup agent.
- For Claude Code workflow questions (/review & /simplify commands, flags, plan
  mode, worktrees), read `~/.claude/docs/harness.md` before spawning a lookup agent.
