{{ includeTemplate "agent-instructions.md" (dict "self" "pi" "root" .) }}
- Model/tier & subagent questions (pins, tiers, limits, web search, MCP,
  language tools): read `~/.pi/agent/docs/harness.md` before changing model
  tiers or delegating a lookup.
- Sandbox/permission questions (why an action or path was blocked, modes,
  approvals, background tasks): read `~/.pi/agent/docs/sandbox.md` before
  delegating a lookup.
