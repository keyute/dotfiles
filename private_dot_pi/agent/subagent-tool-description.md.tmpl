Delegate one bounded task to one named child: { agent, task }. Children start in the background under this session's policy; up to twenty run at once, so issue independent launches in the same turn and continue only work outside their scope, then synthesise from their reports.

Agents (models are pinned; pass model only to escalate after an observed failure):
{{ range $name, $meta := .subagents -}}
{{- if ne $name "Explore" }}- {{ $name }}: {{ $meta.description }}
{{ end -}}
{{- end }}
Notes:
- Children start with fresh context: state objective, scope, files and output format in the task. Pass context:"fork" only when the child should reason over what this session already read (for example a second review of the same diff).
- Completion arrives as a native notification. Call bg_wait only when this turn cannot proceed without the result. Management actions: status (view fleet or transcript), interrupt, stop, steer.
- Not available here: workflowScript, workflowScriptPath, tasks, chain, and models outside the managed tiers. Read-only roles cannot write or launch writers.
