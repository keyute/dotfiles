[user]
	name = Bryan Low
	signingkey = ~/.ssh/signing.pub
[init]
	defaultBranch = main
[core]
	editor = nvim
	pager = delta --features $([ "$(theme-mode 2>/dev/null || echo dark)" = light ] && echo catppuccin-latte || echo catppuccin-mocha)
[pull]
	rebase = true
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[gpg "ssh"]
	allowedSignersFile = ~/.config/git/allowed_signers
{{- if eq .chezmoi.os "darwin" }}
	program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"
{{- else }}
	program = "/opt/1Password/op-ssh-sign"
{{- end }}
[commit]
	gpgsign = true
[diff]
	algorithm = histogram
	colorMoved = default
[gpg]
	format = ssh
[interactive]
	diffFilter = delta --color-only --features $([ "$(theme-mode 2>/dev/null || echo dark)" = light ] && echo catppuccin-latte || echo catppuccin-mocha)
[color]
	ui = true
[push]
	autoSetupRemote = true
[fetch]
	prune = true
[include]
	path = ~/.config/delta/catppuccin.gitconfig
[delta]
	navigate = true
	line-numbers = true
[merge]
	conflictstyle = zdiff3
[rerere]
	enabled = true
[alias]
	# DiffviewOpen is repo-wide, so it can't be a merge.tool: git mergetool
	# invokes per-file with $MERGED and would relaunch it once per conflict
	diffview = "!nvim -c DiffviewOpen"
[includeIf "gitdir:~/Projects/work/**"]
	path = ~/Projects/work/.gitconfig
[includeIf "gitdir:~/Projects/github/**"]
	path = ~/Projects/github/.gitconfig
[includeIf "gitdir:~/Projects/usyd/**"]
	path = ~/Projects/usyd/.gitconfig
