# pi-mcp-adapter 2.36.0 (latest) still declares peerOptional
# @earendil-works/pi-ai@^0.84.1 || ^0.85.0 || ^0.86.0 while the harness pins 0.87.0. npm >= 11.10.1
# resolves that (npm/cli#8981); the npm 10 that Fedora's nodejs22 ships fails
# `npm ci` with ERESOLVE. Drop once pi-mcp-adapter widens its peer range.
legacy-peer-deps=true
