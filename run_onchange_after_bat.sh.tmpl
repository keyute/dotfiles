#!/bin/bash
# tmThemes hash: {{ output "sh" "-c" (printf "cat %s/*.tmTheme 2>/dev/null | sha256sum" (joinPath .chezmoi.homeDir ".config/bat/themes")) | trim }}
command -v bat >/dev/null || exit 0
bat cache --build
