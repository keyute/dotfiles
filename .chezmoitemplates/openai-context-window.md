- OpenAI context window: 272,000 input tokens on the ChatGPT-subscription route,
  inherited from the pinned model catalog — nothing here overrides it. The
  1,050,000 window the model pages document is real but API-key only, where
  input above 272,000 bills the whole request at 2x input / 1.5x output.
- The subscription ceiling has moved without notice, so trust the live context
  readout over any recorded figure, this one included. (Verified 2026-09-10.)
